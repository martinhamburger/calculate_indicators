# GitHub Pages 部署指南

## 📋 概览

这个项目配置了GitHub Actions自动部署前端到GitHub Pages。前端部署后可在GitHub Pages访问，但**后端API需要单独部署**。

## 🚀 部署步骤

### 1. 确保GitHub仓库配置正确

在GitHub仓库中进行以下设置：

1. 访问 **Settings → Pages**
2. **Source**: 选择 "GitHub Actions"
3. **Branch**: 确保主分支是 "main" 或 "master"

### 2. 前端自动部署

推送代码到 `main` 分支后，GitHub Actions会自动：
- ✅ 安装依赖
- ✅ 构建Vite项目
- ✅ 部署到GitHub Pages

在**Actions**标签页可以查看部署进度。

部署完成后访问：`https://your-username.github.io/calculate_indicators/`

### 3. 配置后端API

#### 选项 A：使用远程后端服务器（推荐）

如果你的后端部署在云服务器上（如阿里云、AWS等）：

1. 修改 `frontend/.env.production` 中的 `VITE_API_URL`：
```bash
VITE_API_URL=https://your-backend-server.com:5000
```

2. 确保后端服务器启用了CORS：
```python
# backend/app.py
from flask_cors import CORS
CORS(app)  # 已配置
```

3. 提交并推送更改，GitHub Actions会自动重新部署

#### 选项 B：本地开发

对于本地开发，前端已配置代理到 `http://localhost:5000`。

**本地运行步骤：**
```bash
# 终端1：启动后端
cd backend
python app.py

# 终端2：启动前端
cd frontend
npm run dev
```

访问 `http://localhost:3000`

#### 选项 C：部署后端到云平台

常见的云平台选项：
- **Vercel** - 可部署Node.js应用
- **Railway** - 支持Python Flask
- **Heroku** - 支持Python和Node.js
- **阿里云函数计算** - 支持Python

### 4. 验证部署

部署完成后检查：

```bash
# 检查GitHub Pages URL
https://your-username.github.io/calculate_indicators/

# 验证前端能正常加载
# 检查浏览器开发者工具 (F12) 的 Network 和 Console 标签

# 测试API连接
# 上传文件后检查Network标签，API请求应该指向你配置的后端URL
```

## 🔧 配置文件说明

### `.env.production`
```bash
VITE_API_URL=http://your-backend-server.com:5000
```
生产环境（GitHub Pages）使用的后端API地址

### `.env.development`
```bash
VITE_API_URL=http://localhost:5000
```
本地开发使用的后端API地址

### `.github/workflows/deploy-pages.yml`
自动部署工作流配置，当push到main分支时触发

## 📝 环境变量在代码中的使用

前端代码中获取API地址：
```javascript
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000';
```

所有API请求都使用这个变量作为基础URL：
```javascript
const response = await axios.post(`${API_BASE_URL}/api/calculate`, ...);
```

## ⚠️ 常见问题

### Q: 网页显示正常但计算功能无法使用
**A:** 后端API未正确配置或无法访问。检查：
- `VITE_API_URL` 是否正确指向后端服务器
- 后端服务器是否在运行
- 后端是否启用了CORS
- 浏览器开发者工具中API请求是否返回错误

### Q: 如何更改GitHub Pages的仓库路径
**A:** 修改 `frontend/vite.config.js` 中的 `base` 字段：
```javascript
// 如果仓库名为 'my-repo'
base: process.env.NODE_ENV === 'production' ? '/my-repo/' : '/',
```

### Q: 如何关闭自动部署
**A:** 在仓库 **Settings → Actions → General** 中禁用 Actions 权限，或删除 `.github/workflows/` 目录

## 🔄 工作流程总结

```
┌─────────────────────────────────────┐
│  本地修改代码                        │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  git push 到 main 分支               │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  GitHub Actions 自动触发             │
│  - 安装依赖 (npm ci)                 │
│  - 构建项目 (npm run build)          │
│  - 上传artifact                      │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  部署到 GitHub Pages                 │
│  https://username.github.io/repo/   │
└─────────────────────────────────────┘
```

## 📚 更多资源

- [GitHub Pages 文档](https://docs.github.com/pages)
- [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html#github-pages)
- [GitHub Actions 文档](https://docs.github.com/actions)
